% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recipe-api.R
\name{add_propensity_score_model}
\alias{add_propensity_score_model}
\title{Add an additional model to the propensity score ensemble}
\usage{
add_propensity_score_model(hte_cfg, model_name, ...)
}
\arguments{
\item{hte_cfg}{\code{HTE_cfg} object to update.}

\item{model_name}{Character indicating the name of the model to
incorporate into the propensity score ensemble. Possible values
use \code{SuperLearner} naming conventions. A full list is available
with \code{SuperLearner::listWrappers("SL")}}

\item{...}{Parameters over which to grid-search for this model class.}
}
\value{
Updated \code{HTE_cfg} object
}
\description{
This adds a learner to the ensemble used for estimating propensity
scores.
}
\details{
If the current model is not a \code{SuperLearner} ensemble (e.g. a
\code{GLM_cfg} fallback), it is replaced with a new \code{SLEnsemble_cfg}. This
requires the optional \code{SuperLearner} package and will prompt to install it
if it is missing.
}
\examples{
\dontshow{if (rlang::is_installed("SuperLearner")) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
library("dplyr")
basic_config() \%>\%
   add_propensity_score_model("SL.glmnet", alpha = c(0, 0.5, 1)) -> hte_cfg
\dontshow{\}) # examplesIf}
}
