% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_cfg.R
\name{GLM_cfg}
\alias{GLM_cfg}
\title{Configuration of a Generalized Linear Model}
\description{
\code{GLM_cfg} is a configuration class for estimating a nuisance model with a
single generalized linear model fit with \code{stats::glm.fit}. It requires no
optional packages and is what \code{basic_config()} and \code{HTE_cfg} fall back to
when \code{SuperLearner} is not installed.

This is a deliberately simple model: there is no model selection, no
regularization and no ensembling. Estimates of heterogeneous treatment effects
built on it are only reliable when the linear specification is (close to)
correct. It is intended for prototyping and for minimal installations. A
warning is emitted whenever nuisance models are fit with a \code{GLM_cfg}; use a
\code{SLEnsemble_cfg} (e.g. via \code{add_outcome_model("SL.glmnet")}) for a flexible
ensemble.
}
\examples{
GLM_cfg$new()
GLM_cfg$new(family = stats::binomial())
}
\seealso{
\link{SLEnsemble_cfg}, \code{\link[=basic_config]{basic_config()}}
}
\section{Super class}{
\code{\link[tidyhte:Model_cfg]{tidyhte::Model_cfg}} -> \code{GLM_cfg}
}
\section{Public fields}{
\if{html}{\out{<div class="r6-fields">}}
\describe{
\item{\code{family}}{\code{stats::family} object determining how the GLM is fitted.}

\item{\code{model_class}}{The class of the model, required for all classes
which inherit from \code{Model_cfg}.}
}
\if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-GLM_cfg-new}{\code{GLM_cfg$new()}}
\item \href{#method-GLM_cfg-clone}{\code{GLM_cfg$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-GLM_cfg-new"></a>}}
\if{latex}{\out{\hypertarget{method-GLM_cfg-new}{}}}
\subsection{Method \code{new()}}{
Create a new \code{GLM_cfg} object.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{GLM_cfg$new(family = stats::gaussian())}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{family}}{\code{stats::family} object determining how the GLM should be fitted.
Defaults to \code{stats::gaussian()}, matching the default used for \code{SLEnsemble_cfg}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
A new \code{GLM_cfg} object.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-GLM_cfg-clone"></a>}}
\if{latex}{\out{\hypertarget{method-GLM_cfg-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{GLM_cfg$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
